import Vue from 'vue';

/* Events:
- changeServerUrl($url) - Fired when the server URL has been changed in the front-end and needs to update the model
- addToSource($code) - Adds the code to the source code editor
- updateMapTilesWithUrl($url) (might change later)
*/
export default new Vue();